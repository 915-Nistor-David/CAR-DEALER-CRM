# .NET
bin/
obj/
*.user
.vs/

# Uploaded vehicle photos (runtime content, not source-controlled)
backend/CarFlow.API/wwwroot/vehicles/

# Node
node_modules/
dist/
dist-ssr/

# OS / editor
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Capturi de ecran ad-hoc din radacina (site1.png, site2.png, ...)
site*.png

# Note de sesiune Claude — nu se comit
CLAUDE.md

# Deploy: secretele si datele de pe server. Slash-ul din fata e obligatoriu —
# fara el, `.env` ar prinde si caile din frontend/, care are propriile reguli
# (inclusiv negatia !.env.example).
/.env
/data/
/backups/
